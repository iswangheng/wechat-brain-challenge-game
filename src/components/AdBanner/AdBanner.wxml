<view wx:if="{{show && !adError}}" class="ad-container">
  <ad
    unit-id="{{adUnitId}}"
    ad-intervals="30"
    bindload="onAdLoad"
    binderror="onAdError"
    bindclose="onAdClose"
  ></ad>
</view>
